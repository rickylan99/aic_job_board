<div class="container h-100">

  <div class="col-md-1">
      <a href=<%= admins_index_path %> class="btn btn-outline-primary btn-block">Back</a>
  </div>

  <div class="row justify-content-center">
    <div class="col-8">
      <div class="card" style="padding:2rem">

        <h1 style="text-align:center">Member Resources Video 1</h1>
      

        <%= form_with url: update_resources_path(1), method: :post, model: @resource do |form| %>

          <div class="form-group">
            <label for="job_title">Video Title</label>
            <%= form.text_field :title, value: @stuff.find(1).title, class: "form-control", required: true %>
          </div>
          <div class="form-group">
            <label for="job_position">Video Url</label>
            <%= form.text_field :video_url, value: @stuff.find(1).video_url, class: "form-control", required: true %>
          </div>
          <br/>
          <%= form.submit "Update", class: "btn btn-aggie btn-block" %>
        <% end %>
      </div>

      <br/>
      
      <div class="card" style="padding:2rem">

        <h1 style="text-align:center">Member Resources Video 2</h1>

        <%= form_with url: update_resources_path(2), method: :post, model: @resource do |form| %>

          <div class="form-group">
            <label for="job_title">Video Title</label>
            <%= form.text_field :title, value: @stuff.find(2).title, class: "form-control", required: true %>
          </div>
          <div class="form-group">
            <label for="job_position">Video Url</label>
            <%= form.text_field :video_url, value: @stuff.find(2).video_url, class: "form-control", required: true %>
          </div>
          <br/>
          <%= form.submit "Update", class: "btn btn-aggie btn-block" %>
        <% end %>
      </div>

      <br/>

      <div class="card" style="padding:2rem">

        <h1 style="text-align:center">Member Resources Video 3</h1>

        <%= form_with url: update_resources_path(3), method: :post, model: @resource do |form| %>

          <div class="form-group">
            <label for="job_title">Video Title</label>
            <%= form.text_field :title, value: @stuff.find(3).title, class: "form-control", required: true %>
          </div>
          <div class="form-group">
            <label for="job_position">Video Url</label>
            <%= form.text_field :video_url, value: @stuff.find(3).video_url, class: "form-control", required: true %>
          </div>
          <br/>
          <%= form.submit "Update", class: "btn btn-aggie btn-block" %>
        <% end %>
      </div>

      <br/>

      <div class="card" style="padding:2rem">

        <h1 style="text-align:center">Member Resources Video 4</h1>

        <%= form_with url: update_resources_path(4), method: :post, model: @resource do |form| %>

          <div class="form-group">
            <label for="job_title">Video Title</label>
            <%= form.text_field :title, value: @stuff.find(4).title, class: "form-control", required: true %>
          </div>
          <div class="form-group">
            <label for="job_position">Video Url</label>
            <%= form.text_field :video_url, value: @stuff.find(4).video_url, class: "form-control", required: true %>
          </div>
          <br/>
          <%= form.submit "Update", class: "btn btn-aggie btn-block" %>
        <% end %>
      </div>
          
    </div>
  </div>
</div>